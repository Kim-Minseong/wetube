mixin video(video)
    a(href=`/videos/${video.id}`).video-mixin
        div.video-mixin__thumb(style=`background-image:url(${video.thumbUrl})`)
        div.video-mixin__data
            span.video-mixin__title=video.title
            div.video-mixin__meta
                span #{video.owner.username ? video.owner.username : user.username} • 
                span #{video.meta.views > 1 ? `${video.meta.views} views` : `${video.meta.views} view`}
        
        
            